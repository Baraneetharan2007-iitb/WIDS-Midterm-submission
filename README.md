# PROPAGATION THROUGH THIS REPO:
    This repo contains a python notebook named notebook which contains the code for predicting 
    the close prices of a stock(CEATLTD) in this case using ARIMA model and univariate 
    LSTM Neural Network.
